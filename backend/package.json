{
  "name": "transitops-backend",
  "version": "1.0.0",
  "description": "TransitOps Smart Transport Operations Platform — Backend API",
  "main": "server.js",
  "scripts": {
    "db:init": "psql -U postgres -d transitops -f database/schema.sql && psql -U postgres -d transitops -f database/seed.sql",
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "keywords": ["transitops", "fleet-management", "postgresql"],
  "author": "Krish (Role 1 — Database & DevOps)",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
